/**
 *
 * @param {string} message message to be added to array
 * @param {Array} array Given array to add a message string to it
 */
export default function pushToArray(message, array) {
  array.push(message)
}
